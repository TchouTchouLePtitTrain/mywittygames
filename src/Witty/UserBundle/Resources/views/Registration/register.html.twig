{# <form action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form_register) }} method="POST" class="fos_user_registration_register"> #}
<form action="{{ path('fos_user_security_login') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register">
    {{ form_widget(form) }}
    <div>
        <input id="inscrire" type="submit" value="" />
    </div>
</form>
